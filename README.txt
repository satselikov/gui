extra - unused test files
v1.0 - works for the hard wired boards (not the GPIO reset feature)
v2.0 - works with GPIO feature, not hard wired
v3.0 - split up code for v1.0 
backup - code on lab machine, no ADC, original version used as back up

When running code be sure to do "sudo pigpiod" in the terminal to start the clock process

If setting up to new pi, refer to set up GUIDE. Installing libraries, VNC server for monitorless usage, changing config file etc.

